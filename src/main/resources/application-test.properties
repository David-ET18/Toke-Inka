# =========================================================
# CONFIGURACIÓN PARA EL PERFIL DE PRUEBA ("test")
# =========================================================

# --- BASE DE DATOS EN MEMORIA H2 ---
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# --- SERVICIOS EXTERNOS (DUMMY/DESACTIVADOS) ---

# Mercado Pago (si lo añades en el futuro)
# mercadopago.access-token=TEST-DUMMY-TOKEN

# Correo: Proporcionamos valores falsos para que la configuración no falle.
spring.mail.host=localhost
spring.mail.port=25